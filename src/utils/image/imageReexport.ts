
/**
 * This file reexports the imageService for backward compatibility
 * Can be removed once all imports are updated to the new structure
 */

import { imageService } from "./index";

export { imageService };
